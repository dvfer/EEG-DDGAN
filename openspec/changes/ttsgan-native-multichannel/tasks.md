## 1. Branch setup

- [x] 1.1 Create a new git branch off EEG-DDGAN's current default branch (e.g. `ttsgan-direct`) and confirm no changes are made to the `EEG-GAN` repo. — Created from `origin/mv-train` instead of `main`, per user decision; see `notes.md`.
- [x] 1.2 Record the exact single-channel training command/config currently used (dataset, `n_epochs`, `patch_size`, `hidden_dim`, `use_multiscale_dwt_discriminator`, `use_stacking`, etc.) to serve as the pre-change baseline for the regression check in section 3. — Recorded in `notes.md` (no local dataset/non-AE checkpoint existed; command derived from `pretrained_gan.pt`'s hyperparameters).

## 2. Remove the autoencoder branch from GAN training (`ttsgan-direct-pipeline`)

- [x] 2.1 In `eeggan/helpers/initialize_gan.py::init_gan`, delete the `autoencoder != ''` branch entirely; keep only the plain `TTSGenerator`/`TTSDiscriminator` construction, computing `channel_in_disc`/`sequence_length_generated` solely from the dataloader-derived `n_channels`/`sequence_length`.
- [x] 2.2 Remove the now-unused `TransformerAutoencoder`/`TransformerDoubleAutoencoder`/`consume_prefix_in_state_dict_if_present` autoencoder-loading logic and imports from `init_gan`.
- [x] 2.3 Leave the `discriminator2`/`StackingDiscriminator` construction block at the end of `init_gan` in place, only updating it to read `channel_in_disc` from the simplified (non-autoencoder) computation.
- [x] 2.4 In `eeggan/helpers/system_inputs.py::default_inputs_training_gan`, remove the `autoencoder` CLI argument entry so it is rejected as an unrecognized keyword if passed.
- [x] 2.5 In `eeggan/gan_training_main.py`, remove the `ae_dict = torch.load(opt['autoencoder'], ...)` loading block and the `opt['autoencoder']` field, and drop `autoencoder` from the `opt` dict built from `default_args`.
- [x] 2.6 Grep the codebase for remaining references to `opt['autoencoder']` / `default_args['autoencoder']` (e.g. in `trainer.py`'s `configuration` dict, `generate_samples_main.py`) and remove or neutralize them consistently so no dangling references remain on this branch. — `trainer.py`'s `opt.get('autoencoder')`-guarded line degrades to `None` safely, untouched; `generate_samples_main.py` fixed (see 2.7); `moabb_pipeline.py` fixed (see 2.9).
- [x] 2.7 Add an explicit compatibility check (or confirm an existing one) so that loading a checkpoint with a non-empty `configuration['autoencoder']` fails clearly rather than silently mismatching shapes. — Added explicit `NotImplementedError` in `generate_samples_main.py` before the `init_gan` call; training-resume path already fails via `load_state_dict` key mismatch.
- [x] 2.8 Do NOT touch the feature-matching blocks in `GANTrainer.batch_train` (the `features_fake_d1`/`features_fake_d2` unpacking, the `torch.no_grad()` real-feature computation, and the `lambda_fm * torch.nn.functional.l1_loss(...)` terms) while removing the autoencoder path — confirm after section 2's edits that these blocks are byte-for-byte unchanged from `main`. — Verified via `git diff --stat origin/mv-train -- eeggan/helpers/trainer.py` (empty diff).
- [x] 2.9 (added during implementation) Update `moabb_pipeline.py`, which trained the GAN through the now-removed autoencoder path (`train_gan(..., autoencoder=ae_save_path)`), to train directly on raw multi-channel data: removed `train_autoencoder`/`AE_*` config and the AE training step, dropped the `autoencoder=` kwarg from `train_gan`'s CLI args, added `GAN_USE_STACKING` config.

## 3. Regression check (single-channel baseline)

- [ ] 3.1 Re-run the pre-change baseline command recorded in 1.2 on the modified branch (no `autoencoder` keyword, dual discriminator + stacking enabled) for the same number of epochs.
- [ ] 3.2 Compare discriminator/generator/discriminator2 loss trajectories and the saved checkpoint's `configuration`/`samples` structure against the pre-change run; confirm they match within normal run-to-run variance.
- [ ] 3.3 Specifically confirm the feature-matching term is firing: log or print the L1 feature-matching loss value each `sample_interval` and confirm it is a non-zero, finite, decreasing-or-stable value over training, matching pre-change magnitude for the same config.

## 4. Multichannel wiring (`multichannel-eeg-training`)

- [x] 4.1 Confirm `eeggan/helpers/dataloader.py::Dataloader` correctly reshapes and exposes `self.channels` for a dataset with `N > 1` distinct `kw_channel` values (read/trace the existing `len(self.channels) > 1` branch; add an assertion if the invariant isn't already enforced). — Traced; the `len(df) % len(channels) == 0` assertion and the `view(...).permute(1,2,0)` reshape already generalize correctly to `N > 1`; no change needed.
- [x] 4.2 In `init_gan` (post section 2 cleanup), add explicit shape assertions confirming the generator's output channel dimension and the primary discriminator's input channel dimension both equal the dataloader-derived `n_channels` (+ conditions) for `N > 1`. — Added `assert channel_in_disc == n_channels + n_conditions` and `assert generator.channels == n_channels` in `init_gan`. This caught and fixed a real bug: `generate_samples_main.py` was calling `init_gan(channel_in_disc=n_channels, ...)` without adding `n_conditions`, which the new assertion would have failed for any conditioned model.
- [x] 4.3 Trace `MultiscaleDWTDiscriminator.__init__`/`forward` in `eeggan/nn_architecture/models.py` for `in_channels = N > 1`: confirm `scale_dims_low`/`scale_dims_high` and the per-scale MLP heads are sized correctly, and add a shape assertion after the forward pass confirming `combined_features` dimensions match expectations. — `forward`'s dim-based reshape (already fixed on `mv-train`, see `notes.md`) generalizes correctly; added an input-shape assertion (`x.shape[1] == self.in_channels`) and an output assertion (`combined_features.shape[1] == final_head[0].in_features`).
- [x] 4.4 Trace `WassersteinGradientPenaltyLoss._gradient_penalty` in `eeggan/nn_architecture/losses.py` for real/fake tensors with channel dimension `N + n_conditions > 1`; add a shape/finite-value assertion around the interpolation and gradient computation. — Traced; the dim-count-based permute logic is channel-value-agnostic. Added `assert torch.isfinite(grad_penalty)` before return.
- [x] 4.5 Trace `GANTrainer.make_fake_data`/`batch_train` in `eeggan/helpers/trainer.py` for `n_channels > 1` to confirm label/data concatenation dimensions stay consistent (no hardcoded `1`-channel assumptions). — Traced; `torch.cat(..., dim=-1)` and `data_labels.repeat(1, seq_len, 1)` operate along the channel/sequence axes generically; no hardcoded assumption found, no change needed.
- [x] 4.6 Trace the feature-matching L1 loss specifically for `n_channels > 1`: confirm `features_fake_d1`/`features_fake_d2` and their real-data counterparts (`feats_real_d1`/`feats_real_d2`) have matching shapes when `MultiscaleDWTDiscriminator`/`StackingDiscriminator` are constructed with `in_channels = N > 1`; add a shape assertion immediately before the `l1_loss(...)` calls. — Added both assertions (stacking-branch and secondary-D branch); `torch.nn.functional.l1_loss` silently broadcasts on mismatch rather than erroring, so this was a real gap, not just a formality.

## 5. Multichannel validation

- [ ] 5.1 Obtain or prepare a real multi-electrode EEG dataset (`N > 1` channels) in the expected CSV format (`kw_time`/`kw_channel`/`kw_condition` columns).
- [ ] 5.2 Run a short training run (few epochs) on this dataset using the autoencoder-free pipeline with `use_multiscale_dwt_discriminator` and `use_stacking` enabled; confirm it completes without shape or dtype errors.
- [ ] 5.3 Inspect the resulting checkpoint's `samples` entries and confirm the column structure (conditions, channel label, time-series values) matches the single-channel convention, generalized to `N` channels.
- [ ] 5.4 Document the validated channel count and dataset in the change's notes (or a follow-up doc) as the basis for any further multichannel scaling work.

## 6. Cleanup and open questions

- [x] 6.1 Decide and record whether `vae_training_main.py`/`autoencoder_training_main.py` are deleted from this branch or left present-but-unused (see design.md Open Questions). — Decided: left present-but-unused; out of this change's scope per design.md Non-Goals.
- [x] 6.2 Decide and record whether DDP support for `discriminator2`/`StackingDiscriminator` is in scope for this change or explicitly deferred (see design.md Open Questions). — Decided: deferred; `GANDDPTrainer` untouched, multi-GPU + dual discriminator remains unverified.
- [x] 6.3 Update this branch's README/docs to describe the autoencoder-free, dual-wavelet-discriminator, multichannel-capable pipeline, distinct from `main`'s autoencoder-capable pipeline. — Updated `CLAUDE.md` (project overview, workflow diagram, CLI examples, architecture notes, `moabb_pipeline.py` description, git context).
