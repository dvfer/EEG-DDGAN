## 1. Extract shared metrics module

- [x] 1.1 Create `eeg_metrics.py` at repo root: move `spectral_jsd`, `mmd_rbf`, and the Welch-PSD computation (the `welch(...)` call + `nperseg = min(fs // 4, ...)` logic currently inline in `plot_psd_overlay`) out of `compare_samples.py`, unchanged, into standalone functions (e.g. `compute_psd(trials, fs)` returning `(freqs, mean, std)`).
- [x] 1.2 Add `erp_peak_metrics(real_trials, gen_trials, fs, window_ms=(250, 600))` to `eeg_metrics.py`: trial-averaged peak amplitude + latency inside the window, per design.md D2. Window confirmed against `signal_analysis.ipynb`'s `P300_WIN = (250, 600)` — updated from the initially-assumed 250-500ms.
- [x] 1.3 Update `compare_samples.py` to import `spectral_jsd`, `mmd_rbf`, `compute_psd` from `eeg_metrics.py` instead of defining them inline; `plot_psd_overlay` now calls `compute_psd` per channel/condition instead of calling `welch` directly. No behavior change — verify by re-running `compare_samples.py` on an existing checkpoint and confirming output plots/printed JSD/MMD² are unchanged.
- [x] 1.4 Refactor `compare_subject()` per design.md D1b: add `model_path=None, real_csv=None, gen_csv=None, plot_dir=None` params (each defaulting to today's derived path so `compare_samples.py`'s own CLI is unchanged), and make it return a metrics dict (`jsd_mean`, `mmd2`, `erp_amp_err`, `erp_lat_err_ms`, per condition or averaged) in addition to its existing prints/plots. Call `erp_peak_metrics` (task 1.2) here too.

## 2. Ablation driver script

- [x] 2.1 Create `ablation_pipeline.py` at repo root with the `ABLATIONS` config matrix from design.md D3, plus `ABLATION_SUBJECT`, `ABLATION_N_EPOCHS`, `ABLATION_PATCH_SIZE`, `ABLATION_SEED` constants (flat module-level constants, same style as `moabb_pipeline.py`).
- [x] 2.2 For each config: call `train_gan()` (imported from `moabb_pipeline.py`) with the shared budget + that config's kwarg overrides, saving to `trained_models/ABLATION_<name>_s<subject>.pt` (skip training if the checkpoint already exists, matching `moabb_pipeline.py`'s CSV-exists-skip pattern).
- [x] 2.3 For each trained config: call the refactored `compare_subject()` (imported from `compare_samples.py`, task 1.4) with `model_path`/`gen_csv` pointing at that config's checkpoint/CSV and `plot_dir=ablation_results/<config_name>/` — this produces that config's ERP/PSD/PCA/t-SNE plots and returns its metrics dict.
- [x] 2.4 Collect each config's returned metrics dict into a list as the loop runs (no separate metric computation — reuse task 2.3's return value).
- [x] 2.5 Write `ablation_results/ablation_s<subject>.csv` with one row per config: `config, jsd_mean, mmd2, erp_amp_err, erp_lat_err_ms`.
- [x] 2.6 Plot `ablation_results/psd_comparison_s<subject>.png`: one figure, small multiples per channel, one PSD line per config (using `compute_psd` from `eeg_metrics.py`, loading each config's already-generated CSV from task 2.3) plus the real-data PSD for reference, styled consistently with `compare_samples.py`'s existing `COLORS`/`rcParams` conventions (import them or replicate minimally — reuse rather than reinvent styling).

## 3. Verification

- [x] 3.1 Run `ablation_pipeline.py` end-to-end against a small synthetic/throwaway CSV (not real subject data) with `ABLATION_N_EPOCHS` set very low, confirming all 9 configs train, generate, and produce a non-empty CSV + PNG without errors. Delete throwaway artifacts after. Ran with `ABLATION_SUBJECT=999`, 40 random trials, `patch_size=8`, `n_epochs=2` — all 9 configs trained, generated, plotted (ERP/PSD/PCA/t-SNE per config), and tabulated into `ablation_results/ablation_s999.csv` + `psd_comparison_s999.png` with no errors. Throwaway CSV/checkpoints/results deleted after.
- [x] 3.2 Confirm task 1.3's refactor didn't change `compare_samples.py`'s output: diff printed JSD/MMD² values (or eyeball PSD plots) before/after the refactor on the same checkpoint. No local pre-refactor checkpoint existed to A/B against (real checkpoints live on the remote GPU machine, not this repo) — confirmed instead via (a) `eeg_metrics.py`'s self-check asserting the moved `compute_psd`/`spectral_jsd`/`mmd_rbf` math is unchanged (identical inputs → ~0 JSD/MMD², divergent inputs → higher), and (b) task 3.1's smoke test exercising the exact refactored call path (`plot_psd_overlay` → `compute_psd`) end-to-end with sane, monotonic-looking output.
- [x] 3.3 Real ablation run on the GPU machine, subject 1, production-scale `ABLATION_N_EPOCHS`. Results in `ablation_results/ablation_s001.csv`. Key findings: DWT alone is the strongest single contributor (JSD 0.0012 vs baseline 0.286); PostNet alone also helps hugely but less than DWT (JSD 0.0064); stacking *hurts* relative to DWT-without-stacking on every metric; `lambda_fm` barely moves JSD without PostNet but matters a lot with it (postnet+lambda_fm=0 is one of the worst configs, postnet+lambda_fm=50 is the best-JSD/best-latency config overall) — the nominal "full model" (`lambda_fm=20`) is dominated by `lambda_fm=50` with PostNet.
